@*@model IEnumerable<BCPAO.PhotoManager.Models.Reports>*@
@{
    ViewData["Title"] = "Reports";
}

@section NavBarText {
    <text>Dashboard > Reports</text>
}

<div class="title-bar">
    <h1 class="title-bar-title">
        <span class="d-ib">Reports</span>
        <span class="d-ib">
            <a class="title-bar-shortcut" href="#" title="Add to shortcut list" data-container="body" data-toggle-text="Remove from shortcut list" data-trigger="hover" data-placement="right" data-toggle="tooltip">
                <span class="sr-only">Add to shortcut list</span>
            </a>
        </span>
    </h1>
</div>

@section Breadcrumbs {
    @*<ol class="breadcrumb">
        <li>
            <a asp-controller="Home" asp-action="Index">Dashboard</a>
        </li>
        <li class="active ng-binding">
            <a href="javascript:;">Photos</a>
        </li>
    </ol>*@
}
@section Scripts {
    @*<script type="text/javascript">
        $("[data-toggle=tooltip]").tooltip();
    </script>*@
}

@*foreach (var item in Model)
{
    <img class="photo-hover" src="@string.Format("data:image/jpg;base64,{0}", Convert.ToBase64String(item.ImageData))" alt="" height="234" width="314" />

    @Html.Image(item.ImageData)
}*@





@*<div class="panel mb25">
    <div class="panel-heading">
        <span>Photos</span>
        <span class="pull-right">
            <a asp-action="Create">Upload Photos</a>
        </span>
    </div>
    <div class="panel-body">
        <div class="table-responsive">
            @*<table class="table table-bordered table-striped mb0">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.Email)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.UserName)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Email)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.UserName)
                            </td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@item.Id" data-toggle=tooltip data-placement=top title="Edit"><i class="fa fa-edit"></i></a>
                                &nbsp;
                                <a asp-action="Details" asp-route-id="@item.Id" data-toggle=tooltip data-placement=top title="Details"><i class="fa fa-eye"></i></a>
                                &nbsp;
                                <a asp-action="ChangePassword" asp-route-id="@item.Id" data-toggle=tooltip data-placement=top title="Change Password"><i class="fa fa-key"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>*@
